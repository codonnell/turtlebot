defmodule Turtlebot do
  @moduledoc """
  Documentation for Turtlebot.
  """
  # use Application
  #
  # alias Turtlebot.Consumer
  #
  # def start(_,_) do
  #   import Supervisor.Spec
  #
  #   children = for i <- 1..System.schedulers_online, do: worker(Consumer, [], i)
  #
  #   Supervisor.start_link(children, strategy: :one_for_one)
  # end
end
